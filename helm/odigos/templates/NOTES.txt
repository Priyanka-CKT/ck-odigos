CK KarmaControl installed successfully. For getting started go to the UI by running:

kubectl port-forward svc/ui -n {{ .Release.Namespace }} 3000:3000

or

./KarmaControl ui

Then, go to: http://localhost:3000
