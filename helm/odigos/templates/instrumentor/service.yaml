apiVersion: v1
kind: Service
metadata:
  name: karmatap
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: webhook-server
      port: 9444
      targetPort: 9444
  selector:
    app.kubernetes.io/name: karmatap